name: test_project
version: "1.0.0"
config-version: 2

profile: polars_databricks

seed-paths: ["seeds"]
model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "catalog"

seeds:
  test_project:

models:
  test_project:
    +materialized: table
